## 2026-05-30 - Custom Form Wrappers Accessibility
**Learning:** Custom form wrapper components (like `<Field>`) in this design system must support passing down `htmlFor` to link labels with nested inputs to ensure screen reader accessibility.
**Action:** Always verify custom form wrappers support `htmlFor` when introducing new input elements.
